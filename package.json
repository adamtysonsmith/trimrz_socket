{
  "name": "trimrz_socket",
  "version": "1.0.0",
  "description": "socket server for trimrz api and native clients",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy:development": "git push heroku-development development:master",
    "deploy:production": "git push heroku-production master"
  },
  "author": "Adam Smith <adam@trimrz.com>",
  "license": "private",
  "dependencies": {
    "express": "^4.15.3",
    "socket.io": "^2.0.1"
  }
}
